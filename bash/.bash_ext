# --------- ADD THIS TO .BASH_RC ---------
# if [ -f ~/.config/bash/.bash_ext ]; then
#     source ~/.config/bash/.bash_ext
# fi

if [ -d "$HOME/.local/bin" ]; then
    export PATH="$HOME/.local/bin:$PATH"
fi

eval "$(fzf --bash)"
export FZF_DEFAULT_COMMAND="fdfind . -IH"
eval "$(zoxide init bash)"

mkcd ()
{
    mkdir -p -- "$1" && cd -P -- "$1"
}

enc-fix-keil ()
{
    find . -name '*.h' -exec iconv --verbose -f windows-1251 -t utf-8 -o {} {} \;
    find . -name '*.c' -exec iconv --verbose -f windows-1251 -t utf-8 -o {} {} \;
}

export VISUAL=nvim
export EDITOR=nvim
export SUDO_EDITOR=nvim
export PAGER=cat

alias n='nvim'
alias nv='neovide'
alias gits='git status'
alias gitl='git log -15 --oneline --decorate --graph --branches'
alias nzf='nvim $(fzf)'
alias ll='ls -la'
