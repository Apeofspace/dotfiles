# Ctrl+shift+F5 to instantly reload

# include ashen.conf
# include tokyonight.conf

# font_family family="Maple Mono"
font_family family="Roboto Mono"
# font_family family="Monaspace Argon NF"
# font_family family="Monaspace Krypton NF"
# font_family family="Monaspace Radon NF" # its not very italic in kitty for some reaon

italic_font           auto
bold_font             auto
bold_italic_font      auto
font_size             13.5

cursor_shape          block
cursor_blink_interval 0
cursor_trail          3

background_opacity    0.85
remember_window_size  yes
shell_integration     enabled

tab_bar_min_tabs      2
tab_bar_style         powerline
tab_powerline_style   slanted

# KEYMAPS ( don't work with zellij well )

# close panes/tabs
map alt+q close_window_with_confirmation ignore-shell

# tabs
map alt+t       new_tab_with_cwd
map alt+shift+t detach_window
map alt+left    previous_tab
map alt+right   next_tab

# panes navigation w with nvim integration
action_alias nav kitten pass_keys.py neighboring_window
map ctrl+j    nav bottom ctrl+j
map ctrl+k    nav top    ctrl+k
map ctrl+h    nav left   ctrl+h
map ctrl+l    nav right  ctrl+l
# THESE FUCK WITH TYPING LMAO
# map space>w>j nav bottom ctrl+j
# map space>w>k nav top    ctrl+k
# map space>w>h nav left   ctrl+h
# map space>w>l nav right  ctrl+l
# And allow remote access, otherwise NeoVim can't call back to Kitty.
allow_remote_control yes
listen_on unix:@mykitty

# panes creation
map alt+n       new_window

# reenable layouts in this order (first is default) ctrl+shift+l to switch between them
enabled_layouts tall,grid

# some sessions
startup_session sessions/main.kitty-session
map alt+h goto_session sessions/main.kitty-session
map alt+j goto_session sessions/kpa.kitty-session
map alt+k goto_session sessions/obsidian.kitty-session

# -- EXAMPLES
# # Press F7 and then c to jump to the "cool" project
# map f7>c goto_session ~/path/to/cool/cool.kitty-session
# # Press F7 and then h to jump to the "hot" project
# map f7>h goto_session ~/path/to/hot/hot.kitty-session
# # Browse and select from the list of known projects defined via goto_session commands
# map f7>/ goto_session
# # Same as above, but the sessions are listed alphabetically instead of by most recent
# map f7>/ goto_session --sort-by=alphabetical
# # Go to the previously active session (larger negative numbers jump further back in history)
# map f7>- goto_session -1
